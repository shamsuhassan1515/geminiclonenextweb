，这两个项目分别使用不同的语言和部署方式。以下是在Lima虚拟机的宝塔面板中添加它们的建议：                        
             首先：Lima 默认使用 NAT 网络，需要配置端口转发                                                                                                           
  1. chatgpt-web-midjourney-proxy (前端 + 后端)                                           
                                                                                                                        
  这个项目通常分为 前端 (Frontend) 和 服务层 (Service)。                                                                
   
  - 前端部署 (Node.js/静态):                                                                                            
    - 类型: 建议在本地或服务器运行 pnpm run build，然后将生成的 dist 文件夹内容通过宝塔的 “网站” -> 
  “添加站点”（纯静态）平铺进去。
    - Node 项目: 如果你想直接运行开发环境（不推荐），选 Node.js，启动选项选 npm run dev。
  - 服务层部署 (Node.js 后端 - 在 service 目录下):
    - 语言: 选 Node.js。
    - 项目目录: /Users/aydinkaya/开发/chatgpt-web-midjourney-proxy/service
    - 启动文件/执行文件: 选 package.json。
    - 启动命令: 选 pnpm start 或 node dist/index.js（取决于你是否已经编译）。

  ---
  2. NewApi (API 管理层)

  从目录下的 go.mod 和 main.go 可以看出，这是一个 Go 语言 项目。

  - 语言: 选 Go。
  - 项目目录: /Users/aydinkaya/开发/chatgpt-web-midjourney-proxy/NewApi
  - 执行文件:
    - 你需要先编译。在 NewApi 目录下运行：go build -o new-api main.go。
    - 然后在宝塔中，执行文件 选择刚生成的这个 new-api 文件。
  - 参数: 如果 README 没特别说明，通常不需要额外参数，直接运行即可。

  总结建议：

  1. NewApi: 选 Go。执行文件是你编译出来的二进制文件（通常叫 new-api 或 one-api）。
  2. chatgpt-web-midjourney-proxy (前端): 最好编译后选 静态站点。
  3. chatgpt-web-midjourney-proxy (Service): 选 Node.js。

  注意: 宝塔面板在 macOS 虚拟机里运行时，请确保对应的端口（如 3000, 8000, 3002 等）在 macOS
  主机和虚拟机的防火墙中都已开启。

 Update(.lima/bt-server/lima.yaml)
  ⎿  Added 4 lines
      35      hostPort: 3306
      36    - guestPort: 8888  
      37      hostPort: 8888
      38 +  - guestPort: 3000                                                                                     
      39 +    hostPort: 3000                                                                               
      40 +  - guestPort: 3002                                                                                     
      41 +    hostPort: 3002                                                                                    
      42  
      43  # 使用 virtiofs 文件共享（VZ 驱动支持）
      44  mountType: virtiofs

⏺ 我已经为您配置好了 Lima 虚拟机的端口转发，并整理了宝塔面板的具体配置步骤。
                               
  第一步：重启 Lima 虚拟机使端口转发生效
                                                                                                                  
  由于我刚刚修改了您的 lima.yaml 配置文件，您需要重启虚拟机以启用 3000 和 3002 端口映射。
                                                                                                                  
  在您的 Mac 终端执行：                                                                                         
  limactl stop bt-server
  limactl start bt-server

  ---
  第二步：在 Mac 本地编译项目（关键）

  为了让宝塔能直接运行这些项目，您需要在 Mac 上先生成可执行文件。请在 Mac 终端执行：

  1. 编译 NewApi (Go):
  cd /Users/aydinkaya/开发/chatgpt-web-midjourney-proxy/NewApi
  CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -o new-api-linux main.go                                         
  chmod +x new-api-linux   

  2. 编译服务端 (Node):
  cd /Users/aydinkaya/开发/chatgpt-web-midjourney-proxy/service
  pnpm install
  pnpm run build

  ---
  第三步：在宝塔面板（https://localhost:17604/）中添加项目

  1. 添加 NewApi (Go)

  - 项目类型： Go
  - 项目名称： NewApi
  - 项目目录： /Users/aydinkaya/开发/chatgpt-web-midjourney-proxy/NewApi
  - 执行文件： 点击文件夹图标，选择刚才生成的 /Users/aydinkaya/开发/chatgpt-web-midjourney-proxy/NewApi/new-api-linux
  - 端口： 3000

  2. 添加 MJ Proxy (Node.js)

  - 项目类型： Node.js
  - 项目名称： mj-proxy-service
  - 项目目录： /Users/aydinkaya/开发/chatgpt-web-midjourney-proxy/service
  - 启动选项： npm 或 pnpm
  - 运行脚本： prod (这会运行 node ./build/index.mjs)
  - 端口： 3002

  ---
  验证访问

  - NewApi: 在 Mac 浏览器打开 http://localhost:3000
  - MJ Proxy 服务端: 在 Mac 浏览器打开 http://localhost:3002
  前端静态页  http://localhost:1002/
静态网站需要先通过 Node.js 工具（Vite/Vue）将源代码“翻译”成浏览器能懂的 HTML/JS/CSS。     
                                         
  请在您的 Mac 终端 执行以下命令进行编译：                                                                        
                                                            
  cd /Users/aydinkaya/开发/chatgpt-web-midjourney-proxy                                                           
  # 1. 安装依赖（如果您已经安装过可以跳过）                 
  pnpm install
  # 2. 编译打包
  pnpm run build

  1. 修改网站根目录：将 geminiclone 站点的根目录从 /Users/aydinkaya/开发/chatgpt-web-midjourney-proxy 修改为
  /Users/aydinkaya/开发/chatgpt-web-midjourney-proxy/dist。
  2. 设置伪静态：在站点设置里找到“伪静态”，填入下面的内容（防止刷新页面 404）：
  location / {
      try_files $uri $uri/ /index.html;
  }


宝塔静态页配置文件：server {                                                  
      # 监听 1002 端口 (您在 Mac 访问的端口)
      listen 1002;
      listen [::]:1002;
      server_name 127.0.0.1;

      # 核心：必须指向您项目的编译输出目录 dist
      root /Users/aydinkaya/开发/chatgpt-web-midjourney-proxy/dist;
      index index.html index.htm;

      # 包含宝塔基础配置（可选，主要用于 SSL 等）
      # include /www/server/panel/vhost/nginx/extension/127.0.0.1_80/*.conf;

      # GZIP 压缩优化
      gzip on;
      gzip_proxied any;
      gzip_min_length 1k;
      gzip_comp_level 6;
      gzip_types text/plain text/css application/json application/javascript text/xml application/xml
  application/xml+rss text/javascript;

      # 第一部分：处理静态页面和路由 (Vue/Vite)
      location / {
          try_files $uri $uri/ /index.html;
      }

      # 第二部分：处理后端 API 请求 (转发到 3002 端口)
      location /api {
          proxy_pass http://127.0.0.1:3002;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header REMOTE-HOST $remote_addr;

          # 关键：支持流式传输 (SSE)，让打字机效果不卡顿
          proxy_buffering off;
          proxy_cache off;
          proxy_http_version 1.1;
          proxy_set_header Connection "";

          # 解决部分请求超时问题
          proxy_connect_timeout 60s;
          proxy_send_timeout 600s;
          proxy_read_timeout 600s;
      }

      # 屏蔽敏感文件访问
      location ~* (\.env.*|LICENSE|README\.md|package(-lock)?\.json|pnpm-lock\.yaml|go\.mod|go\.sum)$ {
          return 404;
      }

      # 日志路径（宝塔标准位置）
      access_log /www/wwwlogs/gemini_proxy.access.log;
      error_log /www/wwwlogs/gemini_proxy.error.log;
  }



  - 宝塔面板: 继续使用您当前的 https://localhost:17604

  端口说明： 我已将虚拟机的 3000/3002 与您 Mac 的 3000/3002 一一对应，现在您在虚拟机里开启服务，Mac
  宿主机就能直接访问。

